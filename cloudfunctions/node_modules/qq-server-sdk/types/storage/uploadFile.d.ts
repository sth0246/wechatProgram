/// <reference types="node" />
import { ReadStream } from 'fs';
interface uploadFileOptions {
    cloudPath: string;
    fileContent: Buffer | ReadStream;
}
declare function uploadFile(options: uploadFileOptions, config: any): Promise<{
    fileID: any;
    requestId: any;
    statusCode: number;
}>;
export { uploadFile };
