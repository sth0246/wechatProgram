interface downloadFileOptions {
    fileID: string;
}
declare function downloadFile(options: downloadFileOptions, config: any): Promise<{
    statusCode: number;
    fileContent: any;
}>;
export { downloadFile };
